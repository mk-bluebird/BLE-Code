# .cargo/config.toml
# BLE-Code workspace config for Rust 1.88+ CI

[net]
retry = 2
git-fetch-with-cli = true
offline = false

[build]
target-dir = "target"
incremental = false
rustflags = [
    "-Cdebuginfo=1",
    "-Coverflow-checks=on",
]

[term]
color = "always"
