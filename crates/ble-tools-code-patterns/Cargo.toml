[package]
name = "ble-tools-code-patterns"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_with = "3"
walkdir = "2"
toml = "0.8"
thiserror = "1"
